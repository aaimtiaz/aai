# Normalise to LF in the repo. The admin panel commits Markdown through the
# GitHub API, which always writes LF — without this, checking out on Windows
# would rewrite those files to CRLF and every subsequent edit would show a
# whole-file diff.
* text=auto eol=lf

*.png binary
*.jpg binary
*.webp binary
*.pdf binary
*.woff2 binary
